//The never keyword is used to indicate that a function will not return anything.
//Or that a variable can never have a value.
//The never type is useful for indicating that certain code paths should never
//be reached, or that certain values are impossible. It can help catch errors at
//compile-time instead of runtime.

//I don't wanna learn this. So I skip this part.
//But if you wanna learn this part, just google.
